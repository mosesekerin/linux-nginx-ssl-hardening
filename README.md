# Stage 0 DevOps: Linux Server Setup, Nginx, HTTPS & SSH Hardening

Provisioned a hardened Linux VM, configured Nginx to serve static and JSON endpoints, enforced SSH key-only access, enabled UFW, and secured the domain with Let's Encrypt SSL and HTTP→HTTPS 301 redirect.
